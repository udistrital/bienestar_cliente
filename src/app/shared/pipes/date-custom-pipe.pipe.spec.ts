import { DateCustomPipePipe } from './date-custom-pipe.pipe';

describe('DateCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
